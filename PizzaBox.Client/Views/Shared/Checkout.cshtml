<h2>Than You For Your Order</h2>
<a href="/home">home</a>

@foreach (var item in ViewBag.Order.Pizzas)
{
    <p>Crust: @item.Crust</p>
    <p>Size: @item.Size</p>
    <p>Toppings:</p>

    <ul>
    @foreach (var topper in item.Toppings)
        {
            <li>@topper</li>
        }
    </ul>

}